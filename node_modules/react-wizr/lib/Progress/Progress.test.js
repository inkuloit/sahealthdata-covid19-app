"use strict";

var _react = _interopRequireDefault(require("react"));

var _enzyme = require("enzyme");

var _Progress = _interopRequireDefault(require("./Progress"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

describe('Progress', function () {
  it('returns the completion percentage', function () {
    var percentage = null;
    var context = {
      activeStepIndex: 0,
      totalSteps: 4
    };
    (0, _enzyme.shallow)(_react["default"].createElement(_Progress["default"], {
      render: function render(_ref) {
        var p = _ref.percentage;
        percentage = p;
      }
    }), {
      context: context
    });
    expect(percentage).toEqual(25);
  });
  it('returns the completion percentage', function () {
    var percentage = null;
    var context = {
      activeStepIndex: 1,
      totalSteps: 4
    };
    (0, _enzyme.shallow)(_react["default"].createElement(_Progress["default"], {
      render: function render(_ref2) {
        var p = _ref2.percentage;
        percentage = p;
      }
    }), {
      context: context
    });
    expect(percentage).toEqual(50);
  });
  it('returns the completion percentage', function () {
    var percentage = null;
    var context = {
      activeStepIndex: 2,
      totalSteps: 4
    };
    (0, _enzyme.shallow)(_react["default"].createElement(_Progress["default"], {
      render: function render(_ref3) {
        var p = _ref3.percentage;
        percentage = p;
      }
    }), {
      context: context
    });
    expect(percentage).toEqual(75);
  });
  it('returns the completion percentage', function () {
    var percentage = null;
    var context = {
      activeStepIndex: 3,
      totalSteps: 4
    };
    (0, _enzyme.shallow)(_react["default"].createElement(_Progress["default"], {
      render: function render(_ref4) {
        var p = _ref4.percentage;
        percentage = p;
      }
    }), {
      context: context
    });
    expect(percentage).toEqual(100);
  });
});